fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.6.0
pydantic[email]>=2.9.0
# Import direto em scripts/seed_admin.py — hoje chega via
# pydantic-settings, mas dependência direta se declara (B-B16).
python-dotenv>=1.0.0
sqlalchemy>=2.0.0
pymysql>=1.1.0
cryptography>=42.0.0
alembic>=1.13.0
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
slowapi==0.1.9
# 7 CVEs em 0.0.9 (PYSEC-2026-1851/1852/3036..3040), encontradas pela
# PRIMEIRA execução real do pip-audit no CI em 2026-08-26. É o parser
# de multipart/form-data — ou seja, exatamente o caminho do upload de
# evidências. A mais recente exige 0.0.31.
python-multipart>=0.0.31
aiofiles==23.2.1
structlog>=24.1.0,<27.0.0
reportlab==4.2.5
# PYSEC-2026-1845 exige 9.0.3.
pytest>=9.0.3
httpx==0.27.0
pytest-cov==5.0.0

# Dev apenas — lint (ver backend/ruff.toml)
ruff==0.4.4